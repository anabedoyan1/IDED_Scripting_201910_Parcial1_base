Parcial (2.5/ 5.0)
	* En general, la lógica de equipamiento de partes no se satisface. La propuesta de usar as/is es interesante, pero la mezclaste con el VehicleType y no corregiste eso en el código.
	* El SpeedBonus de las partes únicamente está definido por el Upgrade, por tanto no satisface que las partes configuran sus parámetros por constructor.
	* El accesor MaxSpeed de Part siempre devuelve 0, sin importar qué parámetro se asigna en constructor o qué parte se equipe.
	* Upgrade configura el maxSpeed según el Level. No es que esté mal, pero por encapsulamiento, si el accesor siempre retorna 0, no hay forma de ver que el Vehicle tenga los parámetros esperados. Por otro lado, nada parece cambiar Vehicle.Level de ninguna manera, por lo que siempre Level = 0.
	* Part.Upgrade hace lo mismo, y está el mismo error.
	* El accesor SpeedBonus de Part siempre devuelve 0. Aún cuando un método cambie el valor del atributo, si el accesor siempre retorna el mismo valor, no hay forma de ver que la instancia de PArt tenga los parámetros esperados.
	* No se implementó Durability en las partes.
	

Bonificación (0.0/ 0.5)
	No se presentó propuesta para la bonificación

Total: 2.5/ 5.0